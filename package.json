{
  "name": "lumina-full-stack-template",
  "version": "2.0.0",
  "description": "Modern, production-ready full-stack application template",
  "scripts": {
    "web:dev": "cd vue && npm run dev",
    "api:dev": "cd nest && npm run start:dev",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down -v"
  },
  "author": "John Desjardins",
  "license": "MIT"
}
